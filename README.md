# GitHub-updater-system
Built-in, lightweight update checker that queries the GitHub Releases API to detect newer versions of the application. It has no third-party dependencies (no Sparkle or similar framework required).
